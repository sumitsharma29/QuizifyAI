# ==========================================
# QuizifyAI Environment Configuration (EXAMPLE)
# ==========================================
# IMPORTANT: Never commit your real `.env` file. This example contains
# placeholders only. Create a local `.env` (ignored by git) with real values.

# Gemini API Key (server-side) — keep this private and set it on your server.
# The frontend should NOT have this key. Use the proxy server in `/server` to
# make requests to Gemini from the server-side. Example value for your server
# environment (do NOT commit real keys):
GEMINI_API_KEY=YOUR_SERVER_SIDE_GEMINI_API_KEY

# Firebase configuration (placeholders) — required by src/firebase.js
# Replace these in your local .env file with real project values.
VITE_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN=YOUR_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_PROJECT_ID=YOUR_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET=YOUR_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID=YOUR_FIREBASE_APP_ID

# Optional — Enable debug logs (true/false)
VITE_DEBUG_MODE=false
